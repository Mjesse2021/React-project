import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import "./Style/index.scss";

// il comporte les fichiers index.scss qui a aussi les autres fichiers style de chaque component ou pages @jesse

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('jesse')
);


